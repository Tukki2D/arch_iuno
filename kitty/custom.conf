# custom.conf — kitty personal overrides
# Included from kitty.conf via: include custom.conf
#
# Contains key bindings and any settings not covered by other include files.
# On kitty updates: kitty.conf is replaced with upstream default + include block.
# This file and the others below are untouched by updates.


